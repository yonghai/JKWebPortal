去掉语音订单


1、用户给我一个手机号码，查询是否参加过注册送的活动；
2、配送端的订单列表增加一个参数，该订单是否参与首单送的活动；
3、新增地址增加一个接口（传入姓名、电话、地址），返回地址ID;
4、更新地址增加一个接口；
5、地址列表增加一个接口；
6、默认地址接口的逻辑改一下，如果库里面只有一个地址则就是默认地址，否则最近使用的地址就是默认地址；

以上的接口要跟以前的版本兼容；


配送端增加一个验证注册送活动的入口；
配送端订单列表参与首单送活动的订单可以显示首单送的字样；

客户端用户新增地址，更新地址，地址列表逻辑改变；
客户端注册流程简化；
客户端现有的商品条码录入以及与正在整理的商品规整到一块
客户端一级分类的图片设计；
客户端二级分类图片设计和描述整理；
客户端分类和商品数据替换；
客户端促销广告以及促销内容更换；

地址完全兼容解决方案：
1、修改地址表结构，增加省份、区县、区域、详细地址字段；
2、老版本用户增加新地址，省份、区县、区域、详细地址等信息添加到已经修改的库表结构中；
3、新用户添加信息，详细地址放在新添加的详细地址字段中；


*修改旧版本添加地址的逻辑，讲地址信息添加进新的地址表中